# SafeSecrets Infrastructure - Connection Details
# Copy this to .env.local and fill in your values

# EC2 Instance
EC2_INSTANCE_ID=your_instance_id
EC2_PUBLIC_IP=your_public_ip
EC2_REGION=ca-central-1
EC2_USER=ec2-user

# SSH Key
SSH_KEY_PATH=path/to/your/key.pem

# SSH Connection Command
# ssh -i "${SSH_KEY_PATH}" ${EC2_USER}@${EC2_PUBLIC_IP}
