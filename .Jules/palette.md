## 2025-02-18 - Copy Button Feedback
**Learning:** Adding explicit visual feedback (checkmark icon) to copy actions significantly improves user confidence, especially in non-standard UI elements like the ArtifactPanel notepad.
**Action:** Apply this pattern (click -> action -> success state -> timeout -> reset) to all copy-to-clipboard interactions in the future.
