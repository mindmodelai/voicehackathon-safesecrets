import type { z } from 'zod';
import type { RequestContext } from '../request-context/index.js';
import type { SchemaWithValidation } from '../stream/base/schema.js';
export interface ValidationError<T = any> {
    error: true;
    message: string;
    validationErrors: z.ZodFormattedError<T>;
}
/**
 * Validates raw suspend data against a Zod schema.
 *
 * @param schema The Zod schema to validate against
 * @param suspendData The raw suspend data to validate
 * @param toolId Optional tool ID for better error messages
 * @returns The validated data or a validation error
 */
export declare function validateToolSuspendData<T = any>(schema: SchemaWithValidation<T> | undefined, suspendData: unknown, toolId?: string): {
    data: T | unknown;
    error?: ValidationError<T>;
};
/**
 * Validates raw input data against a Zod schema.
 *
 * @param schema The Zod schema to validate against
 * @param input The raw input data to validate
 * @param toolId Optional tool ID for better error messages
 * @returns The validated data or a validation error
 */
export declare function validateToolInput<T = any>(schema: SchemaWithValidation<T> | undefined, input: unknown, toolId?: string): {
    data: T | unknown;
    error?: ValidationError<T>;
};
/**
 * Validates tool output data against a Zod schema.
 *
 * @param schema The Zod schema to validate against
 * @param output The output data to validate
 * @param toolId Optional tool ID for better error messages
 * @returns The validated data or a validation error
 */
export declare function validateToolOutput<T = any>(schema: SchemaWithValidation<T> | undefined, output: unknown, toolId?: string, suspendCalled?: boolean): {
    data: T | unknown;
    error?: ValidationError<T>;
};
/**
 * Validates request context values against a Zod schema.
 *
 * @param schema The Zod schema to validate against
 * @param requestContext The RequestContext instance to validate
 * @param identifier Optional identifier for better error messages (e.g., tool ID, agent ID)
 * @returns The validated data or a validation error
 */
export declare function validateRequestContext<T = any>(schema: SchemaWithValidation<T> | undefined, requestContext: RequestContext | undefined, identifier?: string): {
    data: T | Record<string, any>;
    error?: ValidationError<T>;
};
//# sourceMappingURL=validation.d.ts.map